# Cloudflare Pages Functions configuration
# Required for the markdown negotiation middleware

compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat_v2"]

[vars]
# No environment variables needed
