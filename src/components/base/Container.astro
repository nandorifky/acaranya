---
interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className = '', narrow = false } = Astro.props;
const maxWidth = narrow ? 'max-w-3xl' : 'max-w-7xl';
---

<div class:list={[maxWidth, 'mx-auto px-4 sm:px-6 lg:px-8', className]}>
  <slot />
</div>
